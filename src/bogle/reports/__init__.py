"""Report engines for the reports epic (issues #25-#30).

Pure aggregation/valuation logic consumed by the CLI commands; one module per
report. Shared period vocabulary lives in :mod:`bogle.reports.periods`.
"""
